name: teamwork
on:
  pull_request:
    types: [opened, closed]
  pull_request_review:
    types: [submitted]
  push:
jobs:
  auto-commeent:
    runs-on: ubuntu-latest
    steps:
      - name: Test Auto Commenter
        uses: zt4ff/auto-commenter@v2
        with:
          type: custom
          body: |
            This is custom message
            It's multiline and it also support github markdown.
            - list item 1
            - llist item 2

            [Link to repo](https://github.com/zt4ff/auto-commenter)
